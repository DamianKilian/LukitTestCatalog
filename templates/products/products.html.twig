

{% for prod in categoryProducts %}
<div style="float:left">
    <div>{{prod.name}}</div>
    <div><img src="/uploads/photos/{{prod.photo}}"></div>
    <div>{{prod.description}}</div>
    <div>{{prod.price}}</div>
</div>
{% endfor %}
